${errorHandler}

#set($resp = $utils.rds.toJsonObject($ctx.result)[1])

#if($resp.size() == 0)
  $utils.error("Couldn't find VerifiedPassenger with slug $ctx.args.slug", "NotFound")
#end

## Should return a VerifiedPassenger type
$utils.toJson($resp[0])
