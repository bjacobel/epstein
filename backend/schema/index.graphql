# import Flight from './types/flight.graphql'
# import Passenger from './Passenger.graphql'
# import FlightConnection from './FlightConnection.graphql'
# import PassengerConnection from './PassengerConnection.graphql'

type Node {
  id: Int
}

type Query {
  flight(id: Int!): Flight
  flights(
    includeLogistical: Boolean = true,
    limit: Int
    offset: Int
  ): FlightConnection

  passenger(id: Int!): Passenger

  searchPassengers(query: String!): PassengerConnection
}
