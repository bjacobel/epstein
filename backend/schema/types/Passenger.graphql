scalar AWSURL
scalar AWSJSON

type Passenger {
  id: Int  # 'literal' passengers will not have ids. ids are only given when they are canonicalized
  slug: String
  literal: String
  name: String
  biography: String
  wikipedia_link: AWSURL
  image: AWSURL
  flightCount: Int
  histogram: AWSJSON
  flights(limit: Int, offset: Int): FlightConnection
}
